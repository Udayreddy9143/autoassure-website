# Copy this file to application-secrets.properties (same folder) and fill in real values.
# application-secrets.properties is gitignored - it never gets committed.
resend.api-key=re_your_real_key_here
